/*
 * Microsoft Application Insights Web Snippet, 1.2.0
 * Copyright (c) Microsoft and contributors. All rights reserved.
 *
 * 
 * 
 *
 * ---------------------------------------------------------------------------
 * This is a single combined (rollup) declaration file for the package,
 * if you require a namespace wrapped version it is also available.
 * - Namespaced version: types/applicationinsights-web-snippet.namespaced.d.ts
 * ---------------------------------------------------------------------------
 */

import { IConfiguration } from '@microsoft/applicationinsights-web';

export declare function getSdkLoaderScript(config: SdkLoaderConfig): string;

declare interface SdkLoaderConfig {
    instrumentationKey?: string;
    connectionString?: string;
    src?: string;
    name?: string;
    ld?: number;
    useXhr?: boolean;
    crossOrigin?: string;
    cfg?: IConfiguration;
    cr?: boolean;
    dle?: boolean;
    sri?: boolean;
}

export declare const webSnippet = "##replaceIKeySnippet##";

export declare const webSnippetCs = "##replaceConnStringSnippet##";

export declare function webSnippetVersion(): string;

export { }
